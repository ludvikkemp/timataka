@model Timataka.Core.Models.Dto.CompetitionInstanceDTO.DevicesDto

@{
    ViewBag.Title = "Devices";
    ViewData["Title"] = "Devices";
    Layout = "_AdminLayout";
}

<section class="content">
    <div class="title-block" style="margin-bottom:15px;">

        <div class="row">
            <div class="col-md-12">
                <span class="title">
                    <a asp-controller="Admin"
                       asp-action="Competition"
                       asp-route-competitionId="@ViewContext.RouteData.Values["competitionId"]">@Model.CompetitionName</a> /

                    <a asp-controller="Admin"
                       asp-action="CompetitionInstance"
                       asp-route-competitionId="@ViewContext.RouteData.Values["competitionId"]"
                       asp-route-competitionInstanceId="@ViewContext.RouteData.Values["competitionInstanceId"]">@Model.CompetitionInstanceName</a> /

                    <a asp-controller="Admin"
                       asp-action="Event"
                       asp-route-competitionId="@ViewContext.RouteData.Values["competitionId"]"
                       asp-route-competitionInstanceId="@ViewContext.RouteData.Values["competitionInstanceId"]"
                       asp-route-eventId="@ViewContext.RouteData.Values["eventId"]">@Model.EventName</a> /

                    Devices
                </span>
            </div>
        </div>

    </div>

    <div class="row">
        <div class="col-md-6">
            <a class="btn btn-primary btn-sm rounded-s"
               asp-controller="Event"
               asp-action="AssignDevice"
               asp-route-eventId="@ViewContext.RouteData.Values["eventId"]">Assign Device</a>
        </div>


    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <section class="section">
                    <div class="col-md-12">
                        <div class="card" style="margin-top:15px;">
                            <section class="example">
                                <div class="table-responsive">
                                    <table class="table table-striped">
                                        <thead>
                                            <tr>
                                                <th>Name</th>
                                                <th>Active</th>
                                                <th>Actions</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            @foreach (var item in Model.DeviceInEventViewModels)
                                            {
                                                <tr>
                                                    <td>@item.DevicesInEvent.Device.Name</td>
                                                    <td>@item.DevicesInEvent.Device.Active</td>
                                                    <td>
                                                        <a class="btn btn-warning-outline btn-sm"
                                                           asp-controller="Event"
                                                           asp-action="UnassignDevice"
                                                           asp-route-deviceId="@item.DevicesInEvent.DeviceId"
                                                           asp-route-competitionId="@ViewContext.RouteData.Values["competitionId"]"
                                                           asp-route-competitionInstanceId="@ViewContext.RouteData.Values["competitionInstanceId"]"
                                                           asp-route-eventId="@item.DevicesInEvent.EventId">Unassign</a>
                                                        <a class="btn btn-success-outline btn-sm"
                                                           asp-controller="Device"
                                                           asp-action="Details"
                                                           asp-route-deviceId="@item.DevicesInEvent.DeviceId">Details</a>
                                                    </td>
                                                </tr>
                                            }
                                        </tbody>
                                    </table>
                                </div>
                            </section>

                        </div>
                    </div>
                </section>
            </div>
        </div>
    </div>
</section>
